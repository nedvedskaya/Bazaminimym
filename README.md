
  # Website Prototype Creation

  This is a code bundle for Website Prototype Creation. The original project is available at https://www.figma.com/design/mYJL72k9foPg54d05ZFor5/Website-Prototype-Creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  